name: flutter_app_size_reducer
description: A Flutter plugin for analyzing and reducing app size through CLI commands.
version: 1.0.3
homepage: https://github.com/saty-a/flutter_app_size_reducer
repository: https://github.com/saty-a/flutter_app_size_reducer
issue_tracker: https://github.com/saty-a/flutter_app_size_reducer/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3
  console_bars: ^1.2.0
  image: ^4.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

dependency_overrides:
  path: ^1.9.0

flutter:
  # This section is intentionally empty as this is a CLI tool

topics:
  - flutter
  - cli
  - assets
  - optimization
  - tools